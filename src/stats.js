// @flow
// const {db} = require('./db')

const recordResolved = async (question: Object) => {
  // const statsCollection = await db('stats')

  // console.log(statsCollection)
}

module.exports = {
  recordResolved
}
